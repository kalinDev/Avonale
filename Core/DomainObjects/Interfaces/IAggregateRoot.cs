namespace Core.DomainObjects.Interfaces;

public interface IAggregateRoot
{
    //Aggregation can be used to model relationships between objects that have a "has-a" relationship, where one object has a reference to another object.
    //For example, a car object may contain references to multiple wheel objects, which are aggregated together to form the car object.
}